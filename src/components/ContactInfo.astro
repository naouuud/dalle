---
import type { SanityDocument } from "sanity";
import { sanityClient } from "sanity:client";

const CONTACT_QUERY = `*[_type == 'contactInfo'][0]`
const contactInfo = await sanityClient.fetch<SanityDocument & { phone: string, email: string}>(CONTACT_QUERY);
const { phone, email } = contactInfo;
---
<div class="contact-info flex flex-col gap-2">
  <a href=`mailto:${email}` class="hover:text-neutral-600 transition-colors">
    { email }
  </a>
  <a href=`tel:${phone}` class="hover:text-neutral-600 transition-colors">
    { phone }
  </a>
</div>

<style>
  .contact-info {
    font-family: "Libre Franklin", system-ui, sans-serif;
    font-weight: 400;
    font-variant-numeric: tabular-nums;
    letter-spacing: 0.01em;
  }
</style>
